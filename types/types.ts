export type MenuData = {
  id: string;
  slug: string;
  type: string;
  title: string;
};

export type CommentType = {
  full_name: string;
  email: string;
  message: string;
  date: string;
  now: string;
};
